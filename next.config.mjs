/** @type {import('next').NextConfig} */
const nextConfig = {
images:{
    remotePatterns: [
        {
          protocol: 'https',
          hostname: 'nazmins28.github.io',
          port: '',
        },
      ],
}


};

export default nextConfig;
